import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  root:{
    alignItems: 'center',
    padding: 30,
    marginTop: 60,
  },
  logo:{
    width: 97,
    maxWidth: 300,
    maxHeight: 200,

  }
})
